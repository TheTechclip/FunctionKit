# url

SSR-safe URL helpers. `toURL` safely parses a URL, `isHttpUrl` requires an absolute HTTP(S) URL with a hostname, and `hostMatchesAny` supports exact host or subdomain matching after normalizing case, `www.`, and a trailing dot.
