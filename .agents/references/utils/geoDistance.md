# geoDistance

SSR-safe Haversine distance helpers. `getDistanceMeters` and `getDistanceKilometers` accept `{ latitude, longitude }` and return `null` for missing, non-finite, or out-of-range coordinates.
